# Pi App Layout

This directory is the packaging-facing home for Raspberry Pi deployment content.

Current canonical Pi-side ownership:
- `legacy_pi/`: Pi-local legacy hardware and workflow modules
- `scripts/`: Pi startup scripts
- `deployment/`: Pi service and config examples

The active FastAPI backend remains in `pi_backend/` for compatibility and entrypoint stability.

